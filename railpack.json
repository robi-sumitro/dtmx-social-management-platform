{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "buildCommand": "pnpm --filter @dtmx/api prisma:generate && pnpm -r build",
    "startCommand": "node dist/apps/api/prisma/heal-migrations.js && pnpm --filter @dtmx/api start:prod"
  }
}
